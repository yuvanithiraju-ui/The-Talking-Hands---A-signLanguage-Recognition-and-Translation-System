# --- Core Deep Learning & Computer Vision ---
tensorflow==2.16.1
keras==3.3.3
opencv-python==4.9.0.80
numpy==1.26.4

# --- Hand Tracking & Utilities ---
cvzone==1.6.1
mediapipe==0.10.14  # Often implicitly used by cvzone/tensorflow
Pillow==10.3.0     # Required for PIL/ImageTk (Tkinter image handling)

# --- Text-to-Speech (TTS) & Audio Playback ---
pyttsx3==2.90
gTTS==2.5.1
playsound==1.3.0

# --- Natural Language Processing (NLP) & Translation ---
pyenchant==3.2.2   # Used for word suggestion/dictionary checks
googletrans==4.0.0-rc1 # Translator library

# --- GUI Framework ---
# Tkinter is built into standard Python installations and does not require a separate installation,
# but the following might be needed for the PIL/Pillow integration.
# pillow is listed above.
